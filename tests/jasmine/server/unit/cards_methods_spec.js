'use strict';

describe('Cards methods', function () {

  it('should insert a new card into the Cards collection', function () {
    spyOn(Cards, 'insert');

    //expect(Cards.insert).toHaveBeenCalled();
  });
});
