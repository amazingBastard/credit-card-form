'use strict';

describe('Root template', function() {

	it('should render the Card Form template if user is logged in', function() {
		// @TODO: write test to check cardForm template render
	});

	it('should render the Login/Register form template if user is not logged in', function() {
		// @TODO: write test to check atForm template render
	});
});
