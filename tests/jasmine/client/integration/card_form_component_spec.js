'use strict';

describe('Card Form component', function() {

	it('should format the card input field into a credit card pattern if the user input is valid', function() {

	});

	it('should flip animate in an icon of a recognized card type as user types inside the card input field', function() {

	});

	it('should turn the card input field a shade of red if the user input is invalid', function() {

	});

	it('should shake animate, display an error message, focus the cursor on the card input, and select the input value if it fails to submit', function() {

	});

	it('should display a success message and disable itself for three seconds if it successfully submits', function() {

	});
});
